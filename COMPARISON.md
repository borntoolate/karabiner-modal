# Vim 版 vs Emacs 版 — 比較と A/B テストのやり方

どちらが体に合うかを判断するための資料です。

---

## 1. 同じ土俵に揃えてあります

両方とも次の点は共通です。**違うのはキー割り当てだけ**なので、比較が公平になります。

- Caps Lock を押している間だけモードに入る（変数方式）
- Caps Lock 単押しは何も起きない
- `Caps + Space` で入力ソース切り替え（⌃Space）
- macOS 標準ショートカット（⌘・⌃・⌥・fn）は一切潰さない
- **選択はモードで行う**（Vim = ビジュアルモード、Emacs = マーク）
- 2ストロークの待ち時間は 0.8 秒
- Caps Lock を離すと全状態がリセットされる
- 保存・閉じるの2ストロークコマンドがある（Vim = `:w` `:q`、Emacs = `C-x C-s` `C-x k`）

選択の方式も揃えたので、**比較の焦点は「キー配置と、覚えていられるか」**に絞られます。

---

## 2. 同じ作業を両方でやると

### カーソルを1つ右へ

| Vim | Emacs |
|---|---|
| `Caps + l` | `Caps + f` |

### 行頭へ

| Vim | Emacs |
|---|---|
| `Caps + ⇧4`（`$` は行末、行頭は `Caps + ⇧6`） | `Caps + a` |

### 単語1つ分、選択しながら進む

| Vim | Emacs |
|---|---|
| `Caps + v` → `Caps + w` | `Caps + ⇧Space` → `Caps + ⇧f` |

### 行末まで削除

| Vim | Emacs |
|---|---|
| `Caps + ⇧d` | `Caps + k` |

### 1行まるごと切り取り

| Vim | Emacs |
|---|---|
| `Caps + d` `d` | `Caps + a` → `Caps + ⇧Space` → `Caps + n` → `Caps + w` |

### 1行まるごと選択

| Vim | Emacs |
|---|---|
| `Caps + ⇧v` | `Caps + a` → `Caps + ⇧Space` → `Caps + n` |

### 文書の末尾まで選択

| Vim | Emacs |
|---|---|
| `Caps + v` → `Caps + ⇧g` | `Caps + ⇧Space` → `Caps + ⇧.` |

### 1画面下へ

| Vim | Emacs |
|---|---|
| `Caps + ⌃f` | `Caps + v` |

### 保存

| Vim | Emacs |
|---|---|
| `Caps + ⇧;` → `w` | `Caps + x` → `s` |

### 検索して次へ

| Vim | Emacs |
|---|---|
| `Caps + /` → `Caps + n` | `Caps + s` |

---

## 3. 設計思想の違い

### 移動キーの配置

**Vim の `hjkl`** は右手ホームポジションに4方向が並びます。
指を動かさずに済むのが最大の利点で、慣れれば速い。
反面、`^` `$` `w` `b` `gg` `G` があちこちに散っていて、
最初のうちは覚えるのに苦労します。

**Emacs の `C-f` `C-b` `C-n` `C-p`** は左手中心で、
`f`（前）と `b`（後）が左右に並んでいないため、方向と指の対応が直感的ではありません。
反面、`a` = 行頭、`e` = 行末、`d` = delete、`k` = kill、`y` = yank、`s` = search、
`w` = 切り取り と、**頭文字が意味と一致している**ので覚えるのは楽です。

要するに **Vim は指の効率、Emacs は記憶の効率**に振っています。

### 選択の作法

どちらもモードを使いますが、モードの粒度が違います。

**Vim のビジュアルモード**は `v`（文字単位）と `⇧v`（行単位）の2種類あり、
行単位のほうは入った瞬間に現在行を選択します。行を扱う作業では手数が少ない。

**Emacs のマーク**は1種類だけで、置いた位置から現在位置までが選択範囲になります。
シンプルですが、「1行選択」のような定型作業でも手で組み立てる必要があります。

### 削除とクリップボード

**Vim 版**は Vim の契約に従い、`dd` はクリップボードへ入ります（`p` で貼り戻せる）。
`⇧d`（`D`）だけは選択して Delete しているだけなので、クリップボードを汚しません。

**Emacs 版**は `C-k` `C-w` `M-w` `M-d` すべてがクリップボードを経由します。
本家の kill ring とは違い保持できるのは直前の1つだけですが、
アプリをまたいで貼り付けられます。

### 修飾キーの使い方

**Vim 版**は Shift も Control も Vim の記法どおりに使います
（`⇧4` = `$`、`⌃f` = 1画面下）。Vim の本を読んだときの表記がそのまま通じます。

**Emacs 版**は Shift が Meta です（`⇧f` = `M-f`）。
本家では Control と Meta が別のキーですが、ここでは
Caps Lock が Control、Caps Lock + Shift が Meta という2段構えになります。

---

## 4. A/B テストのやり方

### 切り替え手順

Karabiner-Elements → Complex Modifications で、

1. 今のルールを **Remove**
2. もう片方を **Add rule** → **Enable**

JSON ファイルは両方 `~/.config/karabiner/assets/complex_modifications/` に
置いたままで構いません。Remove しても消えません。

**両方 Enable にしないでください。** どちらも Caps Lock を掴むので、
先に並んでいるほうだけが動き、もう片方は沈黙します。

### 期間の目安

**片方につき最低3日**、できれば1週間は続けてみてください。
初日は必ず「前のほうが良かった」と感じます。それは慣れの差であって
設計の差ではないので、そこで判断しないほうがいいです。

### 見るべきポイント

- **移動の指の形** — 右手 `hjkl` と、左手 `fbnp`。どちらが速いか
- **覚えていられるか** — 3日後に何も見ずに8割思い出せるのはどちらか
- **選択の粒度** — Vim の `v` / `V` の使い分けが便利か、Emacs のマーク1本で足りるか
- **誤爆の頻度** — 意図しない動作をしてしまう回数
- **アプリをまたいだときの安定感** — ブラウザ、エディタ、Slack で挙動が揺れないか

### 判断がつかなかったら

**両取りもできます。** リーダーキーを分ければ共存可能です。
たとえば Vim 版は Caps Lock、Emacs 版は右 Command にする、といった具合です。
`vim/gen.py` / `emacs/gen.py` のリーダー部分を書き換えて再生成すれば作れます。

ただし「両方使える」は「どちらも身につかない」になりがちなので、
まずは片方に絞って試すことをお勧めします。

---

## 5. 両方に共通する注意点

### 矢印キーがオブジェクト移動になるアプリ

Figma のキャンバス上でオブジェクトを選択している状態だと、
矢印キーはオブジェクトを1px動かします。つまり `Caps + l`（Vim）や
`Caps + f`（Emacs）でオブジェクトが動きます。

除外はしていないので、事故が続くようなら
`frontmost_application_unless` で Figma を除外してください。
やり方は `vim/LIMITATIONS.md` の末尾にあります。

### 読み取り専用のページ

ブラウザで文章を読んでいるだけの状態（テキスト入力欄の外）には
キャレットがないため、選択系の操作は空振りします。
移動キーはスクロールとして機能します。

すでにマウスで選択した範囲があれば、そこから伸ばすことは可能です
（Chromium で実測確認済み。Safari は未検証）。

### ターミナルの中

ターミナル内で Vim や shell のキーバインドを使うときは、
この設定が邪魔になることがあります。気になるなら Terminal / iTerm2 / Warp を
除外アプリに指定してください。
