# ゲームパッド版マニュアル（8BitDo SN30 Pro）

**このファイルは `gen.py` の生成物です。**書き換えても次の `make gamepad` で消えます。割り当てを変えるときは `gen.py` の `BASE` / `MOD_DEFAULT` / `APPS` を、スティックの効きは `DEVICE_SETTINGS` / `XY_SPEED` / `WHEEL_SPEED` を直してください。

8BitDo SN30 Pro を Bluetooth でつないでサブ入力デバイスにします。Caps Lock は使いません。キーボードのキーは一つも潰していないので、Vim モード / Emacs モードと同時に有効にできます。

---

## 使い始める

1. コントローラーを **D-input** にする: `Start + B` を3秒長押し（LED 1 が点滅）。X-input だと Karabiner のデバイス一覧に出ません
2. Mac と Bluetooth でペアリングする
3. Karabiner-Elements → **Devices** → 8Bitdo SN30 Pro の **Modify events** を ON にする
4. `make install-gamepad`（`karabiner.json` に直接書き込みます。剥がすときは `make uninstall-gamepad`）
5. システム設定 → キーボード → **音声入力** を ON にする。ショートカットの設定は何でもよい（L2 は Apple キーボードのマイクキーと同じイベントを送るので、ショートカットに依存しない）
6. システム設定 → キーボード → **キーボードナビゲーション** を ON にする。OFF だと Tab がテキスト欄とリストの間しか動かず、ボタンにフォーカスが移りません

---

## ボタンの位置

<!-- pdf: skip -->

```
   L2 ==================                        ================== R2
   L  ====================                    ==================== R
  +----------------------------------------------------------------+
  |        [UP]                                          (X)       |
  |  [LEFT] -+- [RIGHT]      SELECT   START         (Y)       (A)  |
  |       [DOWN]               (-)     (+)               (B)       |
  |                                                                |
  |                          STAR   HEART                          |
  |               (L3)                          (R3)               |
  |            left stick                    right stick           |
  +----------------------------------------------------------------+
```

| 図の中 | ボタン |
|---|---|
| `L2` / `R2` | 上側の肩ボタン（アナログトリガー） |
| `L` / `R` | 手前側の肩ボタン |
| `UP` `DOWN` `LEFT` `RIGHT` | 十字キー |
| `SELECT` / `START` | Select（−） / Start（＋） |
| `HEART` | ハート（♡） |
| `STAR` | 星（☆）。**Turbo の機能ボタンで、Karabiner からは見えません。**ボタンを押しながら星を押すとそのボタンに連射が付くので、触らないでください |
| `L3` / `R3` | 左 / 右スティックの押し込み |

---

## 常に同じもの

| ボタン | 動作 |
|---|---|
| L2 | 音声入力の開始 / 終了（マイクキーを送出）。押すたびに切り替わる |
| R2 + L2 | 口述の下書きを開く（新しい下書きを作って TextEdit へ）。`dictate` を実行。整形はその TextEdit で（下の「口述から整形まで」） |
| R2 押しっぱなし | レイヤー2。他のボタンの意味が変わる（下の表） |
| 左スティック | マウスカーソル。倒した量だけ動き、0.9 以上に倒して 0.3 秒保持すると動き続ける |
| 右スティック | スクロール。倒して 0.3 秒後から動き続ける |

---

## 単押し（机に向かうアプリ以外はどこでも同じ）

キーボードとマウスの代わりになる操作です。エンジニア業務とブラウジングではアプリによって変わりません。

| ボタン | 送るキー | 動作 |
|---|---|---|
| A | `return` | Enter / 決定 |
| B | `esc` | Esc / 中断 |
| X | `⌦` | Delete（前方削除） |
| Y | `⌫` | Backspace |
| 十字 ↑ | `↑` | ↑ |
| 十字 ↓ | `↓` | ↓ |
| 十字 ← | `←` | ← |
| 十字 → | `→` | → |
| L 押しっぱなし | `⇧` | 本物の修飾キー。十字・クリック・Enter と組み合わせる |
| R | `tab` | Tab（次の要素へ）。L + R で ⇧Tab |
| Select（−） | `⌘C` | コピー |
| Start（＋） | `⌘V` | 貼り付け |
| ハート 単押し | `⌘tab` | 直前のアプリへ。押したまま R で順送り |
| ハート 押しっぱなし | `⌘` | 本物の修飾キー。十字・クリック・スクロールと組み合わせる |
| 左スティック押込 | `マウス左` | 左クリック |
| 右スティック押込 | `マウス右` | 右クリック |

---

## 修飾キーとの組み合わせ

L（⇧）とハート（⌘）は押している間だけ立つ本物の修飾キーなので、個別のルールなしにこう合成されます。実キーボードの修飾キーも同じように効きます。

| 操作 | 動作 |
|---|---|
| L + 十字 | 選択を伸ばす（⇧矢印） |
| L + 左スティック押込 | ⇧クリック（範囲選択） |
| L + A | ⇧Enter（送信せずに改行） |
| L + R | ⇧Tab（前の要素へ。Claude Code では権限モードの切り替え） |
| L + 右スティック | 横スクロール |
| ハート + R | ⌘Tab を続けて送る。ハートを離すまでスイッチャーが出たまま |
| ハート + A | ⌘Enter（Claude Desktop の権限プロンプトでは「今回は許可」。拒否は B の Esc） |
| ハート + 十字 ←→ | 行頭 / 行末（⌘矢印） |
| ハート + 十字 ↑↓ | 文頭 / 文末 |
| ハート + Y | 行頭まで削除（⌘Backspace） |
| ハート + 左スティック押込 | ⌘クリック |
| ハート + 右スティック | ⌘スクロール（対応アプリでズーム） |
| ハート + L + 十字 | 行末 / 行頭まで選択 |

---

## R2 を押しながら（どのアプリでも）

アプリごとに変えてよいのはこの層だけです。「上書き」が「不可」の枠はどのアプリでも同じです。

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| R2 + A | `⌘A` | すべて選択 | 可 |
| R2 + B | `⌘W` | 閉じる | 可 |
| R2 + X | `⌘⇧Z` | やり直す | 不可 |
| R2 + Y | `⌘Z` | 取り消す | 不可 |
| R2 + 十字 ↑ | `PgUp` | 1画面ぶん上へ | 可 |
| R2 + 十字 ↓ | `PgDn` | 1画面ぶん下へ | 可 |
| R2 + 十字 ← | `⌥←` | 1単語左へ | 可 |
| R2 + 十字 → | `⌥→` | 1単語右へ | 可 |
| R2 + L | `⌃⇧tab` | 前のタブ | 可 |
| R2 + R | `⌃tab` | 次のタブ | 可 |
| R2 + Select（−） | `⌘X` | 切り取り | 不可 |
| R2 + Start（＋） | `⌘⇧V` | 書式なしで貼り付け | 可 |
| R2 + ハート | `⌘space` | Spotlight / ランチャー | 不可 |
| R2 + 右スティック押込 | `⌘N` | 新規（新しい書類 / ウィンドウ / セッション） | 可 |

- R2 + 左スティック押込 は既定では何も送りません（アプリ別にだけ置く枠。Claude Desktop の ⌥クリック）。R2 + 右スティック押込 の ⌘N は「新規」の意味がアプリで揃うのでグローバルです（TextEdit / VS Code は新しいファイル、Claude Desktop は新しいセッション、ブラウザやターミナルは新しいウィンドウ）

---

## 口述から整形まで（prompt-refiner）

喋った文を prompt-refiner の `refine` で構造化プロンプトに整形する手順です。prompt-refiner は喋った文を `claude -p` で用途別の構造化プロンプトにする作者の CLI で、このリポジトリには入っていません（呼び出しの契約は README）。起点は R2 + L2 の `dictate` で、新しい下書き（`~/prompt-log/draft-<日時>.md`）を作って TextEdit で開きます。**整形ボタンはその TextEdit にだけあります**（R2 + 十字と Start。並びはキーボードの ⌥⌘1〜5 と同じ）。`refine` はクリップボードを読んで、整形した結果をクリップボードへ書き戻すので、選択 → コピー → 整形 → 貼り付けの形になります。整形には数秒かかり、終わると通知センターに「整形完了」が出ます。

ボタンが実行するのは `$HOME/bin/dictate >/dev/null` と `$HOME/bin/refine code >/dev/null` などで、Karabiner が `/bin/sh -c` で走らせます。コマンドが無ければこの 6 ボタンだけが無音で失敗し、他のボタンは影響を受けません。

| 手順 | ボタン | 送るもの |
|---|---|---|
| 1. 下書きを開く | R2 + L2 | `dictate`（TextEdit が前面に来る） |
| 2. 口述を始める | L2 | マイクキー（音声入力の開始） |
| 3. 喋る | — | — |
| 4. 口述を終える | L2 | マイクキー（音声入力の終了） |
| 5. すべて選択 | R2 + A | `⌘A` |
| 6. コピー | Select（−） | `⌘C` |
| 7a. 整形: Claude Code 向けの依頼文に（⌥⌘1） | R2 + 十字 ↑ | `refine code` |
| 7b. 整形: 調査・壁打ち向けに（⌥⌘2） | R2 + 十字 ↓ | `refine research` |
| 7c. 整形: 文章作成の依頼に（⌥⌘3） | R2 + 十字 ← | `refine doc` |
| 7d. 整形: 起票文に（⌥⌘4） | R2 + 十字 → | `refine ticket` |
| 7e. 清書: Slack などに送る本文に（⌥⌘5） | R2 + Start（＋） | `refine message` |
| 8. 「整形完了」の通知を待つ | — | — |
| 9. 貼り付け先のアプリへ | ハート | `⌘tab` |
| 10. 貼り付け | Start（＋） | `⌘V` |

- **他のアプリで口述した文はキーボードの ⌥⌘1〜5 で整形してください。** 整形ボタンは TextEdit でしか効きません。Claude Desktop、ターミナル / Claude Code、ブラウザの各グループは R2 + A も ⌘A ではないので、コントローラーだけでは完結しません
- **整形中にもう一度押さないでください。** Karabiner はシェルコマンドを同時に1つしか走らせず、次を押すと走っている整形が強制終了されます（R2 + L2 の `dictate` も、prompt-refiner のホットキー ⌥⌘V / ⌥⌘1〜5 も同じ枠です）。終了した整形はクリップボードに何も書きません
- `dictate` は毎回新しい下書きを作ります。前の口述に言い足すボタンはありません（キーボードから `dictate --keep`）
- ⌘A → ⌘C を整形ボタンに畳み込んでいないのは、シェルコマンドが即座に走るのに対してキーイベントはあとから届き、⌘C より先にクリップボードを読んでしまうためです（`DESIGN.md` §5）

---

## Figma（左手デバイス特化）

対象: `com.figma.Desktop`

机に向かって使うアプリなので、単押しもこのアプリ用です。★ が固定ベースと違うところ。ハート（⌘Tab）は変わりません。

### 単押し

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| A | `return` | 中に入る / 編集 | ★ |
| B | `esc` | 抜ける | ★ |
| X | `V` | 移動ツール | ★ |
| Y | `⌘Z` | 取り消し | ★ |
| 十字 ↑ | `↑` | 1px 上へ | ★ |
| 十字 ↓ | `↓` | 1px 下へ | ★ |
| 十字 ← | `←` | 1px 左へ | ★ |
| 十字 → | `→` | 1px 右へ | ★ |
| L | `⌘-` | ズームアウト | ★ |
| R | `⌘=` | ズームイン | ★ |
| Select（−） | `⇧1` | 全体を表示 | ★ |
| Start（＋） | `⇧2` | 選択範囲にズーム | ★ |
| ハート 単押し | `⌘tab` | 直前のアプリへ。押したまま R で順送り |  |
| ハート 押しっぱなし | `⌘` | 本物の修飾キー。十字・クリック・スクロールと組み合わせる |  |
| 左スティック押込 | `マウス左` | 左クリック |  |
| 右スティック押込 | `マウス右` | 右クリック |  |

### R2 を押しながら

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| R2 + A | `⌘D` | 複製 | ★ |
| R2 + B | `⌘⇧Z` | やり直す | ★ |
| R2 + X | `⌘G` | グループ化 | ★ |
| R2 + Y | `⌘⇧G` | グループ解除 | ★ |
| R2 + 十字 ↑ | `⇧↑` | 10px 上へ | ★ |
| R2 + 十字 ↓ | `⇧↓` | 10px 下へ | ★ |
| R2 + 十字 ← | `⇧←` | 10px 左へ | ★ |
| R2 + 十字 → | `⇧→` | 10px 右へ | ★ |
| R2 + L | `⇧0` | 100% 表示 | ★ |
| R2 + R | `⇧2` | 選択範囲にズーム | ★ |
| R2 + Select（−） | `⌘⌥C` | プロパティをコピー | ★ |
| R2 + Start（＋） | `⌘⌥V` | プロパティを貼り付け | ★ |
| R2 + ハート | `⌘space` | Spotlight / ランチャー |  |
| R2 + 右スティック押込 | `⌘N` | 新規（新しい書類 / ウィンドウ / セッション） |  |

---

## Photoshop（左手デバイス特化）

対象: `com.adobe.Photoshop`

机に向かって使うアプリなので、単押しもこのアプリ用です。★ が固定ベースと違うところ。ハート（⌘Tab）は変わりません。

### 単押し

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| A | `B` | ブラシ | ★ |
| B | `E` | 消しゴム | ★ |
| X | `⌘Z` | 取り消し | ★ |
| Y | `I` | スポイト | ★ |
| 十字 ↑ | `⌥]` | 上のレイヤーを選択 | ★ |
| 十字 ↓ | `⌥[` | 下のレイヤーを選択 | ★ |
| 十字 ← | `⌘⌥Z` | 段階的に戻る | ★ |
| 十字 → | `⌘⇧Z` | 段階的に進む | ★ |
| L | `[` | ブラシを小さく | ★ |
| R | `]` | ブラシを大きく | ★ |
| Select（−） | `⌘⇧N` | 新規レイヤー | ★ |
| Start（＋） | `⌘S` | 保存 | ★ |
| ハート 単押し | `⌘tab` | 直前のアプリへ。押したまま R で順送り |  |
| ハート 押しっぱなし | `⌘` | 本物の修飾キー。十字・クリック・スクロールと組み合わせる |  |
| 左スティック押込 | `space` | 押している間だけ手のひらツール（パン） | ★ |
| 右スティック押込 | `マウス右` | 右クリック（ブラシ設定） | ★ |

### R2 を押しながら

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| R2 + A | `V` | 移動ツール | ★ |
| R2 + B | `⌘D` | 選択を解除 | ★ |
| R2 + X | `⌘⇧I` | 選択範囲を反転 | ★ |
| R2 + Y | `G` | 塗りつぶし | ★ |
| R2 + 十字 ↑ | `⌘]` | レイヤーを前面へ | ★ |
| R2 + 十字 ↓ | `⌘[` | レイヤーを背面へ | ★ |
| R2 + 十字 ← | `⌘0` | 画面に合わせる | ★ |
| R2 + 十字 → | `⌘1` | 100% 表示 | ★ |
| R2 + L | `⌘-` | ズームアウト | ★ |
| R2 + R | `⌘=` | ズームイン | ★ |
| R2 + Select（−） | `⌘J` | レイヤーを複製 | ★ |
| R2 + Start（＋） | `⌘⇧S` | 別名で保存 | ★ |
| R2 + ハート | `⌘space` | Spotlight / ランチャー |  |
| R2 + 右スティック押込 | `⌘N` | 新規（新しい書類 / ウィンドウ / セッション） |  |

---

## Illustrator（左手デバイス特化）

対象: `com.adobe.illustrator`

机に向かって使うアプリなので、単押しもこのアプリ用です。★ が固定ベースと違うところ。ハート（⌘Tab）は変わりません。

### 単押し

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| A | `V` | 選択ツール | ★ |
| B | `A` | ダイレクト選択 | ★ |
| X | `⌘Z` | 取り消し | ★ |
| Y | `I` | スポイト | ★ |
| 十字 ↑ | `↑` | ↑ |  |
| 十字 ↓ | `↓` | ↓ |  |
| 十字 ← | `←` | ← |  |
| 十字 → | `→` | → |  |
| L | `⌘-` | ズームアウト | ★ |
| R | `⌘=` | ズームイン | ★ |
| Select（−） | `⌘G` | グループ化 | ★ |
| Start（＋） | `⌘S` | 保存 | ★ |
| ハート 単押し | `⌘tab` | 直前のアプリへ。押したまま R で順送り |  |
| ハート 押しっぱなし | `⌘` | 本物の修飾キー。十字・クリック・スクロールと組み合わせる |  |
| 左スティック押込 | `space` | 押している間だけ手のひらツール（パン） | ★ |
| 右スティック押込 | `マウス右` | 右クリック |  |

### R2 を押しながら

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| R2 + A | `⌘⇧Z` | やり直す | ★ |
| R2 + B | `⌘⇧A` | 選択を解除 | ★ |
| R2 + X | `⌘2` | ロック | ★ |
| R2 + Y | `⌘⌥2` | すべてロック解除 | ★ |
| R2 + 十字 ↑ | `⇧↑` | 大きく上へ | ★ |
| R2 + 十字 ↓ | `⇧↓` | 大きく下へ | ★ |
| R2 + 十字 ← | `⇧←` | 大きく左へ | ★ |
| R2 + 十字 → | `⇧→` | 大きく右へ | ★ |
| R2 + L | `⌘0` | アートボードに合わせる | ★ |
| R2 + R | `⌘1` | 100% 表示 | ★ |
| R2 + Select（−） | `⌘⇧G` | グループ解除 | ★ |
| R2 + Start（＋） | `⌘⇧S` | 別名で保存 | ★ |
| R2 + ハート | `⌘space` | Spotlight / ランチャー |  |
| R2 + 右スティック押込 | `⌘N` | 新規（新しい書類 / ウィンドウ / セッション） |  |

---

## After Effects（左手デバイス特化）

対象: `com.adobe.AfterEffects`

机に向かって使うアプリなので、単押しもこのアプリ用です。★ が固定ベースと違うところ。ハート（⌘Tab）は変わりません。

### 単押し

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| A | `space` | 再生 / 停止 | ★ |
| B | `esc` | 中断 | ★ |
| X | `⌘Z` | 取り消し | ★ |
| Y | `⌘⇧Z` | やり直す | ★ |
| 十字 ↑ | `↑` | 上のレイヤーを選択 | ★ |
| 十字 ↓ | `↓` | 下のレイヤーを選択 | ★ |
| 十字 ← | `PgUp` | 1フレーム戻る | ★ |
| 十字 → | `PgDn` | 1フレーム進む | ★ |
| L | `J` | 前のキーフレームへ | ★ |
| R | `K` | 次のキーフレームへ | ★ |
| Select（−） | `U` | キーフレームを表示 | ★ |
| Start（＋） | `⌘S` | 保存 | ★ |
| ハート 単押し | `⌘tab` | 直前のアプリへ。押したまま R で順送り |  |
| ハート 押しっぱなし | `⌘` | 本物の修飾キー。十字・クリック・スクロールと組み合わせる |  |
| 左スティック押込 | `space` | 再生 / 停止 | ★ |
| 右スティック押込 | `マウス右` | 右クリック |  |

### R2 を押しながら

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| R2 + A | `テンキー0` | プレビュー | ★ |
| R2 + B | `⌘D` | 複製 | ★ |
| R2 + X | `⌘⌥Z` | 段階的に戻る | ★ |
| R2 + Y | `E` | エフェクトを表示 | ★ |
| R2 + 十字 ↑ | `⌘↑` | レイヤーを上へ | ★ |
| R2 + 十字 ↓ | `⌘↓` | レイヤーを下へ | ★ |
| R2 + 十字 ← | `⇧PgUp` | 10フレーム戻る | ★ |
| R2 + 十字 → | `⇧PgDn` | 10フレーム進む | ★ |
| R2 + L | `,` | ズームアウト | ★ |
| R2 + R | `.` | ズームイン | ★ |
| R2 + Select（−） | `⌘⇧S` | 別名で保存 | ★ |
| R2 + Start（＋） | `⌘M` | レンダーキューに追加 | ★ |
| R2 + ハート | `⌘space` | Spotlight / ランチャー |  |
| R2 + 右スティック押込 | `⌘N` | 新規（新しい書類 / ウィンドウ / セッション） |  |

---

## Logic Pro（左手デバイス特化）

対象: `com.apple.logic10`

机に向かって使うアプリなので、単押しもこのアプリ用です。★ が固定ベースと違うところ。ハート（⌘Tab）は変わりません。

### 単押し

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| A | `space` | 再生 / 停止 | ★ |
| B | `return` | 先頭へ戻る | ★ |
| X | `R` | 録音 | ★ |
| Y | `⌘Z` | 取り消し | ★ |
| 十字 ↑ | `↑` | 上のトラックを選択 | ★ |
| 十字 ↓ | `↓` | 下のトラックを選択 | ★ |
| 十字 ← | `,` | 1小節戻る | ★ |
| 十字 → | `.` | 1小節進む | ★ |
| L | `C` | サイクルを ON / OFF | ★ |
| R | `Z` | 選択範囲にズーム / 戻す | ★ |
| Select（−） | `M` | ミュート | ★ |
| Start（＋） | `S` | ソロ | ★ |
| ハート 単押し | `⌘tab` | 直前のアプリへ。押したまま R で順送り |  |
| ハート 押しっぱなし | `⌘` | 本物の修飾キー。十字・クリック・スクロールと組み合わせる |  |
| 左スティック押込 | `マウス左` | 左クリック |  |
| 右スティック押込 | `マウス右` | 右クリック |  |

### R2 を押しながら

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| R2 + A | `⌘S` | 保存 | ★ |
| R2 + B | `⌘⇧Z` | やり直す | ★ |
| R2 + X | `⌘R` | リージョンを繰り返す | ★ |
| R2 + Y | `⌘B` | バウンス | ★ |
| R2 + 十字 ↑ | `⌘↑` | 垂直ズームイン | ★ |
| R2 + 十字 ↓ | `⌘↓` | 垂直ズームアウト | ★ |
| R2 + 十字 ← | `⌘←` | 水平ズームアウト | ★ |
| R2 + 十字 → | `⌘→` | 水平ズームイン | ★ |
| R2 + L | `O` | サイクル範囲を選択範囲に合わせる | ★ |
| R2 + R | `⌘0` | ズームを戻す | ★ |
| R2 + Select（−） | `⌘C` | コピー | ★ |
| R2 + Start（＋） | `⌘V` | 貼り付け | ★ |
| R2 + ハート | `⌘space` | Spotlight / ランチャー |  |
| R2 + 右スティック押込 | `⌘N` | 新規（新しい書類 / ウィンドウ / セッション） |  |

---

## TextEdit（口述の下書き）（R2 を押しながら）

対象: `com.apple.TextEdit`

★ が既定と違うところです。単押しは変わりません。

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| R2 + A | `⌘A` | すべて選択 |  |
| R2 + B | `⌘W` | 閉じる |  |
| R2 + X | `⌘⇧Z` | やり直す |  |
| R2 + Y | `⌘Z` | 取り消す |  |
| R2 + 十字 ↑ | `refine code` | 整形: Claude Code 向けの依頼文に（⌥⌘1） | ★ |
| R2 + 十字 ↓ | `refine research` | 整形: 調査・壁打ち向けに（⌥⌘2） | ★ |
| R2 + 十字 ← | `refine doc` | 整形: 文章作成の依頼に（⌥⌘3） | ★ |
| R2 + 十字 → | `refine ticket` | 整形: 起票文に（⌥⌘4） | ★ |
| R2 + L | `⌃⇧tab` | 前のタブ |  |
| R2 + R | `⌃tab` | 次のタブ |  |
| R2 + Select（−） | `⌘X` | 切り取り |  |
| R2 + Start（＋） | `refine message` | 清書: Slack などに送る本文に（⌥⌘5） | ★ |
| R2 + ハート | `⌘space` | Spotlight / ランチャー |  |
| R2 + 右スティック押込 | `⌘N` | 新規（新しい書類 / ウィンドウ / セッション） |  |

---

## Claude Desktop（R2 を押しながら）

対象: `com.anthropic.claudefordesktop`

★ が既定と違うところです。単押しは変わりません。

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| R2 + A | `⌘N` | 新規セッション（Chat タブでは新規チャット） | ★ |
| R2 + B | `⌘W` | 閉じる |  |
| R2 + X | `⌘⇧Z` | やり直す |  |
| R2 + Y | `⌘Z` | 取り消す |  |
| R2 + 十字 ↑ | `⌘B` | サイドバーの表示 / 非表示 | ★ |
| R2 + 十字 ↓ | `⌃⌘\` | 右に新しいセッションを開く（分割ビュー） | ★ |
| R2 + 十字 ← | `⌘⌥←` | 前のサーフェス（Code → Cowork → Chat） | ★ |
| R2 + 十字 → | `⌘⌥→` | 次のサーフェス（Chat → Cowork → Code） | ★ |
| R2 + L | `⌘⇧[` | 前のセッション（サイドバーの1つ上） | ★ |
| R2 + R | `⌘⇧]` | 次のセッション（サイドバーの1つ下） | ★ |
| R2 + Select（−） | `⌘X` | 切り取り |  |
| R2 + Start（＋） | `⌘K` | セッションを検索 / 開始（コマンドパレット） | ★ |
| R2 + ハート | `⌘space` | Spotlight / ランチャー |  |
| R2 + 左スティック押込 | `⌥マウス左` | ⌥クリック（サイドバーのセッションを分割ビューで開く） | ★ |
| R2 + 右スティック押込 | `⌘N` | 新規（新しい書類 / ウィンドウ / セッション） |  |

- 既存のセッションを開くのは、サイドバーの項目を左スティックで指して左スティック押込（クリック）か、R2 + L / R で1つずつ送る。**分割ビューで開くときは R2 を握ったまま押し込む**（⌥クリック）
- 新規セッションは R2 + A でも R2 + 右スティック押込（どのアプリでも ⌘N）でも同じ
- 権限プロンプトは ハート + A（⌘Enter）で今回は許可、B（Esc）で拒否。Claude の応答を止めるのも B
- サーフェスの切り替え（⌘⌥←→）と分割ビュー（⌃⌘\）は Code タブ以外でも押せるが、分割ビューは Code タブにしかない
- セッション番号で飛ぶ ⌘1〜9、Changes / ターミナル / ブラウザのペイン切り替え（⌘⇧D / ⌘J / ⌘⇧B）はキーボードのまま。ペインはヘッダーのアイコンをクリックしても開く

---

## ターミナル / Claude Code（R2 を押しながら）

対象: `com.apple.Terminal`, `com.lambdalisue.Arto`, `com.microsoft.VSCode`, `com.googlecode.iterm2`, `com.mitchellh.ghostty`, `dev.warp.Warp-Stable`, `com.github.wez.wezterm`

★ が既定と違うところです。単押しは変わりません。

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| R2 + A | `⌥return` | 改行（送信しない） | ★ |
| R2 + B | `⌃C` | 強制中断 | ★ |
| R2 + X | `⌘⇧Z` | やり直す |  |
| R2 + Y | `⌘Z` | 取り消す |  |
| R2 + 十字 ↑ | `PgUp` | 1画面ぶん上へ |  |
| R2 + 十字 ↓ | `PgDn` | 1画面ぶん下へ |  |
| R2 + 十字 ← | `⌥←` | 1単語左へ |  |
| R2 + 十字 → | `⌥→` | 1単語右へ |  |
| R2 + L | `⌃⇧tab` | 前のタブ |  |
| R2 + R | `⌃tab` | 次のタブ |  |
| R2 + Select（−） | `⌘X` | 切り取り |  |
| R2 + Start（＋） | `⌘K` | 画面をクリア | ★ |
| R2 + ハート | `⌘space` | Spotlight / ランチャー |  |
| R2 + 右スティック押込 | `⌘N` | 新規（新しい書類 / ウィンドウ / セッション） |  |

- VS Code の新規ファイル（⌘N）は R2 + 右スティック押込（どのアプリでも同じ）

---

## ブラウザ（R2 を押しながら）

対象: `com.google.Chrome`, `com.apple.Safari`, `org.mozilla.firefox`, `com.brave.Browser`, `company.thebrowser.Browser`

★ が既定と違うところです。単押しは変わりません。

| ボタン | 送るキー | 動作 | 上書き |
|---|---|---|---|
| R2 + A | `⌘T` | 新規タブ | ★ |
| R2 + B | `⌘W` | 閉じる |  |
| R2 + X | `⌘⇧Z` | やり直す |  |
| R2 + Y | `⌘Z` | 取り消す |  |
| R2 + 十字 ↑ | `⌘L` | アドレスバー（検索欄）を選択 | ★ |
| R2 + 十字 ↓ | `⌘⌥B` | ブックマークの一覧を開く（Chrome: ブックマーク マネージャー / Safari: ブックマークを編集） | ★ |
| R2 + 十字 ← | `⌘[` | 戻る | ★ |
| R2 + 十字 → | `⌘]` | 進む | ★ |
| R2 + L | `⌃⇧tab` | 前のタブ |  |
| R2 + R | `⌃tab` | 次のタブ |  |
| R2 + Select（−） | `⌘X` | 切り取り |  |
| R2 + Start（＋） | `⌘R` | リロード | ★ |
| R2 + ハート | `⌘space` | Spotlight / ランチャー |  |
| R2 + 右スティック押込 | `⌘N` | 新規（新しい書類 / ウィンドウ / セッション） |  |

- ブックマークの一覧では、項目を左スティックで指してクリックで選び、**押込を2回（ダブルクリック）で開く**。A（Enter）は名前の変更になる（Chrome / Safari とも）。Chrome は ハート + A（⌘Enter）で新しいタブに開く。X / Y は選んだブックマークの削除なので押さない（消したら R2 + Y で取り消す）
- ブックマーク バーを常に出しておくなら ⌘⇧B（Chrome / Safari とも）。キーボードから一度切り替えれば、以後はバーの項目を1クリックで開ける
- 新規ウィンドウは R2 + 右スティック押込（⌘N）

---

## スティックの効き

<!-- pdf: skip -->

Karabiner はスティックを2通りで動かします。傾きが増えたぶんだけその場で動く**ナッジ**と、しきい値以上に倒して保持すると動き続ける**連続移動**です。連続移動はしきい値を超えてから **0.3 秒後**に始まります。この 0.3 秒は Karabiner のソースにハードコードされていて、どの設定でも消せません。しきい値を超えた瞬間にナッジは止まるので、しきい値をデッドゾーンと同じにすると倒した瞬間に 0.3 秒の空白が始まります（`DESIGN.md` §6）。

タイマーは両スティックで1本です。片方が動き続けている間、もう片方は効きません。

| 項目 | いま | Karabiner 既定 | 効果 |
|---|---|---|---|
| カーソル 連続移動に入るしきい値 | 0.9 | 1.00 | ここ以上に倒して 0.3 秒保持で連続移動。超えた瞬間にナッジは止まる |
| カーソル 連続移動の更新間隔 | 10 ms | 20 ms | 動き出してからの粒度。動き出すまでの待ちには効かない |
| カーソル デッドゾーン | 0.05 | 0.10 | 下げると軽い。ドリフトが出たら 0.12〜0.2 に上げる |
| カーソル速度 flick / slow | 150 / 13 | 16 / 8 | flick = ナッジ（傾きの増分 × flick px）/ slow = 連続移動（傾き × slow px を更新ごと） |
| スクロール 連続移動に入るしきい値 | 0.05 | 1.00 | デッドゾーンと同じ値なら、倒して 0.3 秒後から動き続ける |
| スクロール 連続移動の更新間隔 | 10 ms | 10 ms | 動き出してからの粒度 |
| スクロール デッドゾーン | 0.05 | 0.10 | 下げると軽い。ドリフトが出たら上げる |
| スクロール速度 flick / hold | 1.0 / 0.15 | 1 / 0.1 | flick = ナッジ / hold = 連続移動 |

`mid` / `fast` は、連続移動中にもう片方のスティックも倒したときだけ効きます（傾きは 1.0 で頭打ちで、そこにもう片方の傾きが足されるため）。

---

## ボタンと HID イベントの対応

Karabiner-EventViewer で実機から取得した値です。並びが連番ではないので、推測でルールを書くとずれます。

| ボタン | イベント |
|---|---|
| A | `pointing_button: button1` |
| B | `pointing_button: button2` |
| X | `pointing_button: button4` |
| Y | `pointing_button: button5` |
| L | `pointing_button: button7` |
| R | `pointing_button: button8` |
| L2 | `pointing_button: button9` |
| R2 | `pointing_button: button10` |
| Select（−） | `pointing_button: button11` |
| Start（＋） | `pointing_button: button12` |
| ハート | `pointing_button: button13` |
| 左スティック押込 | `pointing_button: button14` |
| 右スティック押込 | `pointing_button: button15` |
| 十字 ↑ | `generic_desktop: dpad_up` |
| 十字 ↓ | `generic_desktop: dpad_down` |
| 十字 ← | `generic_desktop: dpad_left` |
| 十字 → | `generic_desktop: dpad_right` |
| 星 | 何も出ない（Turbo の機能ボタン） |

`button3` と `button6` はこのコントローラーには存在しません。十字キーだけ種別が `generic_desktop` です。`from` に書けることは公式ドキュメントに記載がありませんが、実機で確認しています。

---

## 注意

- **エンジニア業務とブラウジングでは単押しを変えません。** アプリ固有の操作は `gen.py` の `APPS` に R2 の上書きとして足します（既定のある 10 枠と、既定のない左スティック押込。取り消し / やり直し / 切り取り / Spotlight は上書き不可）。机に向かうアプリだけ `DESK_APPS` で単押しも上書きします。ハートはどこでも ⌘Tab です
- **長押しで繰り返すのは矢印・PgUp / PgDn・Backspace / Delete だけです。**他のボタンは押し続けても1回しか出ません。⇧Tab は L を先に押してから R です
- **シェルコマンド（R2 + L2 の `dictate`、TextEdit の整形）は1つずつ。** Karabiner 全体で同時に1つだけで、次を押すと走っているほうが強制終了されます。整形結果の先頭が Karabiner のログに残らないよう、標準出力は捨てています（エラーは `~/.local/share/karabiner/log/console_user_server.log` に出ます）
- **作り直した割り当ては実機未検証です。** 送るキーはこの表のとおりなので、動きが違うときは `DESIGN.md` §9 と突き合わせてください
- **A ボタンは実マウスの左クリックと同じイベント**（`button1`）です。すべてのルールに `device_if` が必要で、付け忘れるとトラックパッドのクリックが死にます。JSON を手で書かず、必ず `gen.py` から生成してください
- EventViewer は Karabiner が変換したあとのイベントを表示します。ルールのあるボタンを押すと、ボタン番号ではなく送ったキーが出ます

